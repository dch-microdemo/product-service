apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: product-app
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: product-deploy
  minReplicas: 1
  maxReplicas: 4
  targetCPUUtilizationPercentage: 50
